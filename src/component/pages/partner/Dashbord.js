import React from 'react';

const Dashbord = () => {
    const id = sessionStorage.getItem('restaurantId');
    // alert(id);
    return (
        <div>
            <h4>Dashbord</h4>
        </div>
    );
};

export default Dashbord;